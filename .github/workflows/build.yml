on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string
      os:
        required: true
        type: string
      buildArgs:
        required: true
        type: string
      artifactName:
        required: true
        type: string
      artifactPath:
        required: true
        type: string
      cachePath:
        required: true
        type: string
      cacheLibrariesPath:
        required: true
        type: string
      exportPath:
        required: true
        type: string

env:
  HAXE_VERSION: 4.3.6
  PROJECT_NAME: PSliceEngine

jobs:
  build:
    name: Build
    runs-on: ${{ inputs.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Setup Haxe
        uses: krdlab/setup-haxe@master
        with:
          haxe-version: ${{ env.HAXE_VERSION }}
          
      - name: Set XCode app
        if: inputs.os == 'macos-15'
        run: sudo xcode-select -s /Applications/Xcode_16.4.app

      - name: Restore Libraries for compilation
        id: load-lib-cache
        uses: actions/cache@v3
        with:
          key: ${{ inputs.cacheLibrariesPath }}
          fail-on-cache-miss: true
          enableCrossOsArchive: true
          path: |
            .haxelib/
          restore-keys: |
            ${{ inputs.cacheLibrariesPath }}

      - name: Restore existing build cache for faster compilation
        id: load-cache
        uses: actions/cache@v3
        with:
          key: ${{ inputs.cachePath }}
          path: |
            export/release/${{ inputs.exportPath }}/haxe/
            export/release/${{ inputs.exportPath }}/obj/
          restore-keys: |
            cache-build-${{ inputs.exportPath }}

      - name: Install libVLC
        if: inputs.exportPath == 'linux'
        run: sudo apt install libvlc-dev libvlccore-dev

      - name: Configure Android
        if: inputs.exportPath == 'android'
        env:
          PLAYSTORE_FIREBASE: ${{ secrets.PLAYSTORE_FIREBASE }}
        run: |
          haxelib run lime config ANDROID_SDK $ANDROID_HOME
          haxelib run lime config ANDROID_NDK_ROOT $ANDROID_NDK_HOME
          haxelib run lime config JAVA_HOME $JAVA_HOME_17_X64
          haxelib run lime config ANDROID_SETUP true
          printf '%s' "$PLAYSTORE_FIREBASE" > ./setup/google-services.json

      - name: Configure iOS
        if: inputs.name == 'iOS'
        env:
          PLAYSTORE_FIREBASE_IOS: ${{ secrets.PLAYSTORE_FIREBASE_IOS }}
        run: printf '%s' "$PLAYSTORE_FIREBASE_IOS" > ./setup/GoogleService-Info.plist

      # PASO 1: GENERAR EL PROYECTO ANDROID (Compila C++ y crea carpetas base)
      - name: Generate Android Build Files
        if: inputs.exportPath == 'android'
        run: haxelib run lime build android -nosign || true

      # PASO 2: INTERCEPTAR Y RESPALDAR LA CARPETA ANTES DE QUE LIME LA TOQUE
      - name: Intercept and Copy Build Folder
        if: inputs.exportPath == 'android'
        run: |
          echo "Haciendo copia de seguridad del proyecto generado para burlar a Lime..."
          cp -r export/release/android/bin ./android-clean

      # PASO 3: REMIENDO EN LA CARPETA SEGURA (Aquí Lime ya no puede sobreescribir nada)
      - name: Fix Firebase Crashlytics Bug in Clean Folder
        if: inputs.exportPath == 'android'
        run: |
          FILE="./android-clean/app/build.gradle"
          if [ -f "$FILE" ]; then
            echo "Mutilando bloque de Firebase corrupto en copia segura: $FILE"
            sed -i '/firebaseCrashlytics\s*{/,/}/d' "$FILE"
            sed -i '/firebaseCrashlytics/d' "$FILE"
          fi

      # PASO 4: COMPILACIÓN DIRECTA POR GRADLE PARA ANDROID (Bypasseando a Lime)
      - name: Compile APK via Gradle Direct
        if: inputs.exportPath == 'android'
        run: |
          cd android-clean
          chmod +x gradlew
          ./gradlew assembleRelease
          
          # Devolvemos el APK generado a la ruta original para no romper los pasos de firmado de abajo
          mkdir -p ../../../export/release/android/bin/app/build/outputs/apk/release/
          cp app/build/outputs/apk/release/*.apk ../../../export/release/android/bin/app/build/outputs/apk/release/
          
          # Renombrar adecuadamente si Lime o Gradle le cambiaron el nombre base
          cd ../../../export/release/android/bin/app/build/outputs/apk/release/
          if [ -f "app-release.apk" ]; then
            mv app-release.apk PSliceEngine-release.apk
          fi

      # PASO 5: COMPILACIÓN NORMAL (Solo corre para Windows, Linux o iOS)
      - name: Compile
        if: inputs.exportPath != 'android'
        run: haxelib run lime build ${{ inputs.buildArgs }}

      - name: Upload debugging symbols (Android)
        if: inputs.exportPath == 'android' 
        run: sh ./setup/firebase_spell.sh

      - name: Upload debugging symbols (iOS)
        if: inputs.name == 'iOS'
        run: sh ./setup/firebase_ios.sh

      - name: Sign APK with the proper key
        if: inputs.exportPath == 'android'
        env:
          OUT_DIR: ./export/release/android/bin/app/build/outputs/apk/release/
          SIGNING_KEY: ${{ secrets.MIKOLKA_PASSWORD }}
          SIGNING_ALIAS: pslice
          SIGNING_FILE: ${{ secrets.MIKOLKA_KEYSTORE }}
        run: |
          rm -f ./key.keystore;
          echo $SIGNING_FILE | base64 -d > ./key.keystore;
          sha256sum ./key.keystore;

          mv '${{env.OUT_DIR}}PSliceEngine-release.apk' '${{env.OUT_DIR}}PSliceEngine-unsigned.apk';
          $ANDROID_HOME/build-tools/35.0.1/zipalign -P 16 -f -v 4 '${{env.OUT_DIR}}PSliceEngine-unsigned.apk' '${{env.OUT_DIR}}PSliceEngine-release.apk' > /dev/null;

          $ANDROID_HOME/build-tools/35.0.1/apksigner sign --ks key.keystore --ks-key-alias $SIGNING_ALIAS --ks-pass "env:SIGNING_KEY" --key-pass "env:SIGNING_KEY" '${{env.OUT_DIR}}PSliceEngine-release.apk';
          rm '${{env.OUT_DIR}}PSliceEngine-unsigned.apk';

      - name: Make IPA
        if: inputs.name == 'iOS'
        run: |
          cd export/release/ios/build/Release-iphoneos
          mkdir Payload
          mv *.app Payload
          zip -r ${{ env.PROJECT_NAME }}.ipa Payload

      - name: Upload Artifact
        uses: actions/upload-artifact@main
        with:
          name: ${{ inputs.artifactName }}
          path: ${{ inputs.artifactPath }}
          if-no-files-found: error    

      - name: Clearing already existing cache
        uses: actions/github-script@v6
        with:
          script: |
            const caches = await github.rest.actions.getActionsCacheList({
              owner: context.repo.owner,
              repo: context.repo.repo,
            })
            for (const cache of caches.data.actions_caches) {
              if (cache.key == "${{ inputs.cachePath }}") {
                console.log('Clearing ' + cache.key + '...')
                await github.rest.actions.deleteActionsCacheById({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  cache_id: cache.id,
                })
                console.log("Cache cleared.")
              }
            }

      - name: Uploading new cache
        uses: actions/cache@v3
        with:
          key: ${{ inputs.cachePath }}
          path: |
            export/release/${{ inputs.exportPath }}/haxe/
            export/release/${{ inputs.exportPath }}/obj/
          restore-keys: |
            ${{ inputs.cachePath }}
            
